# Base URL of the FastAPI backend. Leave unset to use the dev proxy in
# vite.config.ts (which forwards /auth, /stock, /extract, ... to this URL).
# Set it explicitly for a production build where the API is on another origin.
VITE_API_BASE_URL=http://127.0.0.1:8000
